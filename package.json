{
  "name": "travel-planner",
  "private": true,
  "packageManager": "pnpm@11.7.0",
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "dev": "concurrently \"pnpm api\" \"pnpm web\"",
    "api": "pnpm --filter api dev",
    "web": "pnpm --filter web dev"
  },
  "devDependencies": {
    "only-allow": "^1.2.2"
  }
}
